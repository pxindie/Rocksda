//class windows {
//  String type;
  
//  windows(String Type){
//  type = Type;
//  }
  
//  void button(int x,int y,int size, int triangle,String write){
//    triangle(x,y,x,y+size,x+size,y+size/2);
//  }

//  float wpsizer(float x,float y,String u){
//    float z = 0;
//    float xsub = x - width;
//    float ysub = y - height;
//    if(xsub>ysub){
//      z = xsub / ysub;
//      ysub = height;
//      xsub = ysub*z;
//    } else if(xsub<ysub){
//      z = ysub / xsub;
//      xsub = width;
//      ysub = xsub*z;
//    } else{
//      xsub = width;
//      ysub = height;
//    }
//    if(u=="x"){ z = xsub;}
//    if(u=="y"){ z = ysub;}
    
//    return z;
//  }

//}
